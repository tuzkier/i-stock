"""Shared helpers for HarnessV2 hook checks.

contracts  — contract YAML load / save / unwrap / pending-recheck flags
commands   — harness CLI command parsing + dangerous git detection
runtime    — mission-status, gate status, traces, overlay, approvals
paths      — glob path matching
roles      — reviewer / worker / data-producer role classification
"""
